# Dependencies
**/node_modules

# Secrets — never commit
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
*.swp

# Build output
build/
dist/
legacy/build/
legacy/node_modules/
.turbo/

# Logs + runtime artifacts
*.log
*.jsonl

# Data snapshots (contain order/inventory data)
shopify-active-products.json
shopify-operations-snapshot.json
square-snapshot.json

# Local database (contains sessions + mirrored data)
legacy/prisma/dev.sqlite
legacy/prisma/dev.sqlite-journal
web/storage/*.sqlite*
web/db/*.sqlite*

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Rails generated types
.react-router/
node_modules/.cache/
