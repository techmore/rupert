# frozen_string_literal: true

source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "~> 4.0"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 8.1.3"

# The original asset pipeline for Rails [https://github.com/rails/sprockets-rails]
gem "sprockets-rails"

# Use PostgreSQL as the database for Active Record
gem "pg", "~> 1.5"

# sqlite3 retained for the one-time legacy import (db:import_legacy)
gem "sqlite3", "~> 2.1"

# Use the Puma web server [https://github.com/puma/puma]
gem "puma", "~> 6.0"

# Use JavaScript with ESM import maps [https://github.com/rails/importmap-rails]
gem "importmap-rails"

# Hotwire's SPA-like page accelerator [https://turbo.hotwired.dev]
gem "turbo-rails"

# Hotwire's modest JavaScript framework [https://stimulus.hotwired.dev]
gem "stimulus-rails"

# Build JSON APIs with ease [https://github.com/rails/jbuilder]
gem "jbuilder"

# Use Redis adapter to run Action Cable in production
gem "redis", "~> 4.0"

# Use Kredis to get higher-level data types in Redis [https://github.com/rails/kredis]
# gem "kredis"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
# gem "bcrypt", "~> 3.1.7"

gem "tzinfo-data"

# Reduces boot times through caching; required in config/boot.rb
gem "bootsnap", require: false

# Load .env from the repo root into ENV at boot
gem "dotenv-rails"

# Password authentication for admins/tenants
gem "bcrypt", "~> 3.1.7"

# Tailwind CSS (v3 via tailwindcss-rails) for the Oatmeal-themed UI
gem "tailwindcss-rails"

# Background jobs for scheduled syncs
gem "solid_queue"

# Google Drive backups (Settings page)
gem "google-apis-drive_v3"
gem "signet"

# --- ERP framework gems ---
# Fast pagination
gem "pagy", "~> 8.6"
# Search / sort / filter for index pages
gem "ransack"
# Time-series grouping (group_by_hour/day/week/month)
gem "groupdate"
# Simple, dependency-light charts for dashboards and KPIs
gem "chartkick"
# State machines for orders, payments, tasks, projects
gem "aasm"
# Policy objects for module-level authorization
gem "pundit"
# Soft deletes (keep history on financial/CRM records)
gem "discard"
# Audit trail / versioning of critical records
gem "paper_trail"
# In-app + email notifications
gem "noticed"
# Excel export for ledger/reports/sales
gem "caxlsx_rails"
# Faster JSON serialization
gem "oj"

# Use Sass to process CSS
# gem "sassc-rails"

# Use Active Storage variants [https://guides.rubyonrails.org/active_storage_overview.html#transforming-images]
# gem "image_processing", "~> 1.2"

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem "debug", platforms: [:mri, :mingw, :x64_mingw]
end

group :development do
  # Use console on exceptions pages [https://github.com/rails/web-console]
  gem "web-console"

  # Add speed badges [https://github.com/MiniProfiler/rack-mini-profiler]
  # gem "rack-mini-profiler"

  # Speed up commands on slow machines / big apps [https://github.com/rails/spring]
  # gem "spring"

  gem "rubocop"
  gem "rubocop-shopify"
end

group :test do
  # Use system testing [https://guides.rubyonrails.org/testing.html#system-testing]
  gem "capybara"
  gem "selenium-webdriver"
  gem "webdrivers"
  gem "webmock"
  gem "mocha"
  gem "minitest", "~> 5.25"
  gem "factory_bot_rails"
  gem "faker"
end

gem "shopify_app", "~> 23.0"
