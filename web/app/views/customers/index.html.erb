<% content_for :title, "Customers" %>
<div class="space-y-6">
  <section class="flex items-end justify-between gap-4">
    <div>
      <h1 class="font-display text-3xl text-ink">Customers</h1>
      <p class="mt-1 text-sm text-mocha">One unified view of every customer across channels.</p>
    </div>
    <% if Current.user.can?("customers.write") %>
      <%= link_to "Add customer", new_customer_path, class: "btn-primary" %>
    <% end %>
  </section>

  <section class="grid grid-cols-2 gap-4 lg:grid-cols-4">
    <div class="stat">
      <p class="stat-label">Customers</p>
      <p class="stat-value mt-1"><%= @pagy.count %></p>
      <p class="mt-1 text-xs text-taupe">across all channels</p>
    </div>
  </section>

  <%= search_form_for @q, url: customers_path, class: "flex flex-wrap items-center gap-3" do |f| %>
    <%= f.search_field :first_name_or_last_name_or_email_or_phone_cont, placeholder: "Search name, email, phone…", aria_label: "Search customers", class: "search w-64" %>
    <%= f.submit "Search", class: "btn-ghost btn-sm" %>
    <%= link_to "Reset", customers_path, class: "text-xs font-semibold text-olive hover:underline" %>
  <% end %>

  <div class="card overflow-hidden">
    <div class="overflow-x-auto">
      <table class="w-full text-sm">
        <thead class="border-b border-fog bg-haze/60">
          <tr>
            <th scope="col" class="th">Name</th>
            <th scope="col" class="th">Email</th>
            <th scope="col" class="th">Phone</th>
            <th scope="col" class="th">Source</th>
            <th scope="col" class="th text-right">Orders</th>
            <th scope="col" class="th text-right">Lifetime</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-fog">
          <% if @customers.empty? %>
            <tr>
              <td class="td py-8 text-center text-taupe">No customers yet — run a sync to import them.</td>
            </tr>
          <% end %>
          <% @customers.each do |customer| %>
            <tr class="hover:bg-cream">
              <td class="td font-medium text-ink"><%= link_to customer.name, customer_path(customer), class: "hover:text-olive hover:underline" %></td>
              <td class="td text-mocha"><%= customer.email.presence || "—" %></td>
              <td class="td text-mocha"><%= customer.phone.presence || "—" %></td>
              <td class="td"><span class="pill-taupe capitalize"><%= customer.source %></span></td>
              <td class="td text-right text-mocha"><%= customer.orders.size %></td>
              <td class="td text-right font-medium text-ink"><%= number_to_currency((@lifetime_totals[customer.id] || 0) / 100.0) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <div class="border-t border-fog px-5 py-3 text-xs text-taupe">
      <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
    </div>
  </div>
</div>
