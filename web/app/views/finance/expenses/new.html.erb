<% content_for :title, "Record expense" %>
<div class="space-y-6">
  <section>
    <h1 class="font-display text-3xl text-ink">Record expense</h1>
    <p class="mt-1 text-sm text-mocha">Money out — supplies, shipping, labor, rent, and more.</p>
  </section>
  <div class="card card-pad max-w-2xl">
    <%= render "form", expense: @expense %>
  </div>
</div>
