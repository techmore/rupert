<% content_for :title, "Edit #{@location.name}" %>
<div class="space-y-6">
  <section>
    <h1 class="font-display text-3xl text-ink">Edit <%= @location.name %></h1>
  </section>
  <div class="card card-pad max-w-xl">
    <%= render "form", location: @location %>
  </div>
</div>
