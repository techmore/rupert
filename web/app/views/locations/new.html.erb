<% content_for :title, "Add location" %>
<div class="space-y-6">
  <section>
    <h1 class="font-display text-3xl text-ink">Add location</h1>
    <p class="mt-1 text-sm text-mocha">A manual stock location for the shop (synced locations come from Shopify/Square automatically).</p>
  </section>
  <div class="card card-pad max-w-xl">
    <%= render "form", location: @location %>
  </div>
</div>
