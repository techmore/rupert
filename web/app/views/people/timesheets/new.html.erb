<% content_for :title, "New timesheet" %>
<div class="space-y-6">
  <section>
    <h1 class="font-display text-3xl text-ink">New timesheet</h1>
    <p class="mt-1 text-sm text-mocha">Pick an employee and the pay week, then add daily hours.</p>
  </section>
  <%= render "form", timesheet: @timesheet %>
</div>
