<% content_for :title, "New purchase order" %>
<div class="space-y-6">
  <section>
    <h1 class="font-display text-3xl text-ink">New purchase order</h1>
    <p class="mt-1 text-sm text-mocha">Start a draft; add lines, then place it with the vendor.</p>
  </section>
  <div class="card card-pad max-w-2xl">
    <%= render "form", purchase_order: @purchase_order %>
  </div>
</div>
