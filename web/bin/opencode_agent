#!/usr/bin/env ruby
# frozen_string_literal: true

$stdout.sync = true

require_relative '../config/environment'

Rails.logger = Logger.new($stdout)
Rails.logger.level = Logger::INFO

OpencodeChatAgent.run!
